class DisplayDay extends React.Component {
    render() {
        return React.createElement(
            "h1",
            null,
            this.props.items
        );
    }
}

export default DisplayDay;